///Single item of donor appointments added in calendar_screen_data.dart.
///Holds date [day] and donation type [appointment].
class Appointment {
  final DateTime day;
  final String appointment;

  Appointment(this.day, this.appointment);
}